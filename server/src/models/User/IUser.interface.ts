/**
 * User Data, interface (contract)
 *
 * Defines contrat to use type of data User
 *
 * @version 1.0.0
 */
export interface IUser {
    name: String;
    surname: String;
    email: String;
    username: String;
    password: String;
}